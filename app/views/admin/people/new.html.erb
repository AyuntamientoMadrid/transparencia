<%= render 'admin/people/hidden_rows'%>

<%= form_for @person, url: admin_people_path, html: { multipart: true, 'data-abide' => '' } do |f| %>
  <%= render 'admin/people/main_form', f: f %>
<% end %>
