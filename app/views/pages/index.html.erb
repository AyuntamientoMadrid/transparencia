<%= link_to t("pages.create"), new_page_path, class: 'button radius small right margin-top' %>

<nav class="breadcrumbs" role="menubar" aria-label="breadcrumbs">
  <li role="menuitem">
    <%= link_to t("home"), root_path %>
  </li>
  <li role="menuitem" class="unavailable" role="button" aria-disabled="true">
    ¿Qué buscas?
  </li>
</nav>

<div class="browse-panes row ">

  <% @pages_path.each do |page| %>
    <%= render 'pages/section', page: page %>
  <% end %>

  <%= empty_sections_placeholders(@pages_path) %>
</div>
