<%= form_for @objective do |f| %>
  <%= f.text_field :title %>
  <%= f.trix_editor :description %>
  <%= f.check_box :accomplished %>
  <%= f.submit t('shared.update'), class: "button radius success" %>
<% end %>
