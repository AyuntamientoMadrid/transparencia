<a name="activities_declarations" data-magellan-destination="activities_declarations"></a>
<h2 class="lead"><%= t("people.activities_declarations") %></h2>

<ul class="tabs" data-tab role="tablist">
  <% @activities_declarations.each_with_index do |declaration, index| %>
    <% active = index == @activities_declarations.length - 1 %>
    <li class="tab-title <%= 'active' if active %>">
      <a href="#activities_declaration_<%= iso_date(declaration.declaration_date) %>"><%= declaration.declaration_date %></a>
    </li>
  <% end %>
</ul>

<div class="tabs-content">
  <% @person.activities_declarations.each_with_index do |declaration, index| %>

    <% active = index == @activities_declarations.length - 1 %>
    <div role="tabpanel" id="activities_declaration_<%= iso_date(declaration.declaration_date) %>"
         aria-hidden="<%= !active %>"
         class="content <%= 'active' if active %>">

      <table>
        <caption>1) Puestos de trabajo, cargos o actividades en el sector público</caption>
        <thead>
          <tr>
            <th scope="col" class="th-big">Entidad</th>
            <th scope="col">Cargo o categoría</th>
            <th scope="col" class="th-small">Fecha inicio</th>
            <th scope="col" class="th-small">Fecha cese</th>
          </tr>
        </thead>
        <tbody>
          <% declaration.public_activities.each do |activity| %>
            <tr>
              <td><%= activity.entity %></td>
              <td><%= activity.post %></td>
              <td><%= format_date activity.start_date %></td>
              <td><%= format_date activity.end_date %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <table>
        <caption>2) Actividades privadas por cuenta propia o ajena </caption>
        <thead>
          <tr>
            <th scope="col">Clase</th>
            <th scope="col">Descripción</th>
            <th scope="col">Entidad/Colegio profesional</th>
            <th scope="col">Cargo o categoría</th>
            <th scope="col" class="th-small">Fecha inicio</th>
            <th scope="col" class="th-small">Fecha cese</th>
          </tr>
        </thead>
        <tbody>
          <% declaration.private_activities.each do |activity| %>
            <tr>
              <td><%= activity.kind %></td>
              <td><%= activity.description %></td>
              <td><%= activity.entity %></td>
              <td><%= activity.post %></td>
              <td><%= format_date activity.start_date %></td>
              <td><%= format_date activity.end_date %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <table>
        <caption>3) Otras actividades y/o supuestos de posible incompatibilidad</caption>
        <thead>
          <tr>
            <th scope="col">Descripción</th>
            <th scope="col" class="th-small">Fecha inicio</th>
            <th scope="col" class="th-small">Fecha cese</th>
          </tr>
        </thead>
        <tbody>
          <% declaration.other_activities.each do |activity| %>
            <tr>
              <td><%= activity.description %></td>
              <td><%= format_date activity.start_date %></td>
              <td><%= format_date activity.end_date %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

    </div>
  <% end %>
</div>
