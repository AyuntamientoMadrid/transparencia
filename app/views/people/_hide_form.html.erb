<div id="<%= dom_id(person) %>_hide_form" style="display: none;">
  <%= form_for person, url: hide_person_path(person), method: :put do |f| %>
    <%= f.text_field :hidden_reason %>
    <%= f.submit t('shared.submit')%>
  <% end %>
</div>
